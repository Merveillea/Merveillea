version: '3.7'
services:
    back:
        container_name: backend
        build:
            context: ./backend
            dockerfile: Dockerfile
        volumes:
            - ./backend:/backend
        ports:
            - '8080:8080'